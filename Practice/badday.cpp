#include<bits/stdc.h>
using namespace std;
int main()
{
    string s;
    cin>>s;
    int n = s.length();
    vector <int> v;
    vector <int> s;
    for(int i=0;i<n;i++)
    {
        v.push_back(i);
    }
    int c=0;
    while(v.size()!=0 || s.size()!=0 
    {
        while(v.size()!=0)
        {
            if(c==0 && v[front])
            {

            }
        }
    }
}